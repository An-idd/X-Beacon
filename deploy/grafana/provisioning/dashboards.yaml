apiVersion: 1

providers:
  - name: x-beacon
    orgId: 1
    folder: X-BEACON
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
